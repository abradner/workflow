# CLAUDE.md

Claude Code reads this file automatically at the start of every session. The
actual guidance lives in `AGENTS.md`, which is the cross-tool convention other
agentic editors read too — keeping one canonical file avoids two sets of
instructions drifting apart and contradicting each other.

@AGENTS.md
